[package]
name = "arbit-fund-session"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
casper-contract = { version = "5", default-features = false }
casper-types = { version = "6", default-features = false }
